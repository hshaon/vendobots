import 'package:flutter/material.dart';

class MapPage extends StatelessWidget {
  const MapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Map View: Navigation and SLAM Display',
      style: TextStyle(fontSize: 18),
    );
  }
}